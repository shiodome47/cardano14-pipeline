---
layout: layouts/base.njk
title: "Fund14 Proposals"
---

<h1 class="text-2xl font-semibold mb-2">
  CardanoCatalyst Fund14 提案一覧
</h1>
<p class="text-sm text-carda-textSub mb-6">
  Project Catalyst Fund14 の提案を、日本語タイトルとメタ情報つきで一覧表示しています。
</p>

<div class="mb-4 flex flex-wrap items-center gap-2 text-xs">
  <span class="px-2 py-1 rounded-full bg-carda-primarySoft/20 text-carda-textMain">
    全 {{ f14 | length }} 件
  </span>
</div>

<div class="grid gap-4 md:grid-cols-2">
  {% for p in f14 %}
    <article class="group rounded-2xl border border-carda-primarySoft/40 bg-carda-surface p-4 shadow-sm hover:shadow-md hover:border-carda-primary/70 transition-all">
      <header class="mb-2">
        <h2 class="text-sm font-semibold leading-snug text-carda-textMain">
          <a
            href="/proposals/{{ p.proposal_id | lower }}/"
            class="hover:underline underline-offset-2"
          >
            {{ p.title_ja or p.title_en }}
          </a>
        </h2>
       <p class="mt-1 text-[11px] text-carda-textSub">
          {{ p.proposal_id }} · Fund {{ p.fund }}
        </p>

        {% if p.challenge %}
          <p class="mt-1">
            <span class="inline-flex items-center rounded-full bg-carda-primarySoft/20 text-[10px] text-carda-primary px-2 py-0.5">
              {{ p.challenge }}
            </span>
          </p>
        {% endif %}
      </header>

      {% if p.summary_ja %}
        <p class="text-xs text-carda-textSub line-clamp-3">
          {{ p.summary_ja }}
        </p>
      {% else %}
        <p class="text-xs text-carda-textSub italic">
          サマリー準備中…
        </p>
      {% endif %}

      <dl class="mt-3 flex flex-wrap gap-x-4 gap-y-1 text-[11px] text-carda-textSub">
        <div>
          <dt class="uppercase tracking-wide text-carda-textSub/80">Requested</dt>
          <dd class="font-medium text-carda-textMain">
            {{ p.requested_ada or "-" }}
          </dd>
        </div>

        {% if p.votes_cast %}
          <div>
            <dt class="uppercase tracking-wide text-carda-textSub/80">Votes</dt>
            <dd class="font-medium text-carda-textMain">
              {{ p.votes_cast }}
            </dd>
          </div>
        {% endif %}

        {% if p.meets_approval_threshold %}
          <div>
            <dt class="uppercase tracking-wide text-carda-textSub/80">Approval</dt>
            <dd class="font-medium text-carda-textMain">
              {{ p.meets_approval_threshold }}
            </dd>
          </div>
        {% endif %}
      </dl>

      {% if p.status %}
        <div class="mt-2 text-[11px]">
          {% set st = p.status | lower %}
          {% if st == "funded" %}
            <span class="inline-flex items-center rounded-full bg-emerald-100 text-emerald-700 px-2 py-0.5">
              FUNDED
            </span>
          {% else %}
            <span class="inline-flex items-center rounded-full bg-slate-200 text-slate-700 px-2 py-0.5">
              {{ p.status }}
            </span>
          {% endif %}

          {% if p.not_funded_reason %}
            <span class="ml-2 text-[10px] text-carda-textSub">
              ({{ p.not_funded_reason }})
            </span>
          {% endif %}
        </div>
      {% endif %}

      {% if p.proposal_url %}
        <div class="mt-2 text-[11px]">
          <a
            href="{{ p.proposal_url }}"
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-1 text-carda-primary underline underline-offset-2"
          >
            原文（Project Catalyst）を見る
            <span aria-hidden="true">↗</span>
          </a>
        </div>
      {% endif %}
    </article>
  {% endfor %}
</div